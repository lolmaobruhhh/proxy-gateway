{
  "name": "proxy-gateway",
  "version": "1.0.0",
  "type": "module",
  "description": "Universal AI Proxy Gateway — provider-agnostic relay with dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.1",
    "cors": "^2.8.5"
  }
}
